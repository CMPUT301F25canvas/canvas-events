package com.example.lotteryeventsystem.model;

/**
 * Tracks where an entrant sits in an event waitlist.
 */
public enum WaitlistStatus {
    WAITING,
    INVITED,
    CONFIRMED,
    DECLINED,
    CANCELLED
}
